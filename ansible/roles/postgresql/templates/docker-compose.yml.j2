version: '3'
services:
  postgresql:
    image: '{{ postgresql_image }}'
    container_name: harbor-db
    restart: always
    ports:
      - 5432:5432
    volumes:
      - {{ db_data_dir }}:/var/lib/postgresql/data:z
    env_file:
     - ./config/env
    dns_search: .
